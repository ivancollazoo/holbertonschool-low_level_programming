#include "main.h"

/**
 * mul - Write a function that multiplies two integers.
 * Return: Always 0.
 * @a: results
 * @b: results
 */

int mul(int a, int b)
{

	for (a = 0; a < b; a++)

	{
		a = b + a;
	}

	return (0);

}
