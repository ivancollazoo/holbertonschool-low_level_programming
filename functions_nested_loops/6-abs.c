#include "main.h"
#include <stdio.h>

/**
 * int _abs - a function that computes the absolute value of an integer.
 * Return: Always 0.
 */

int _abs(int)
{
int x;

	x = _abs (7);
	printf("absolute value of x = %d\n", x);

	return(0);
}	
